projects:
  twitterclone:
    schemaPath: ../../../graphql/schema.json
    includes:
      - graphql/**/*.ts
    excludes:
      - ./amplify/**
    extensions:
      amplify:
        codeGenTarget: typescript
        generatedFileName: Api.ts
        docsFilePath: /graphql
extensions:
  amplify:
    version: 3
